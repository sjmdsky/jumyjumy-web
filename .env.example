# 后端 API 根地址。前端 /q/<query> 在服务端转发到 <API_BASE_URL>/q/<query>（返回 JSON）。
#
# 该变量在 astro.config.mjs 的 env.schema 中声明，页面通过 `astro:env/server` 类型安全地读取。
# access: 'secret' 意味着它在【运行时】解析，改地址不需要重新构建。
#
# 本文件只服务于本地 `astro dev` / `astro build`。
# 线上（Cloudflare Workers）不读 .env，值来自 wrangler.jsonc 的 vars
# （或同名 secret：`wrangler secret put API_BASE_URL`，secret 优先级更高），
# 由适配器在每次请求时拷进 process.env。`wrangler dev` 同样走 wrangler.jsonc。
#
# 两处都没有配置时，回落到 schema 里的 default（http://localhost:3000）。
API_BASE_URL=http://localhost:3000

# 与后端共享的网关调用密钥（对应后端的 APP__RATE_LIMIT__TRUSTED_CLIENT_IP_TOKEN）。
# 可选。配置后随 x-real-ip 一起在 x-gateway-token 请求头中转发，用于网关鉴权与访客真实 IP 背书。
# 线上环境使用：`wrangler secret put GATEWAY_TOKEN`
# 本地如需测试网关鉴权可解除注释：
# GATEWAY_TOKEN=

